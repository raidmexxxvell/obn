Flask==2.3.3  # downgraded for better Flask-SocketIO compatibility
gspread==6.0.0
google-auth==2.23.4
python-dotenv==1.0.0
SQLAlchemy==2.0.36
flask-compress==1.14
requests==2.31.0

# Оптимизации производительности
redis==5.0.1
flask-socketio==5.3.6
python-socketio==5.11.0

# Database system dependencies
psycopg[binary]==3.2.9  # PostgreSQL adapter for Python 3.13
alembic==1.13.2  # Database migrations (optional)

# Security and monitoring (Phase 3)
psutil==5.9.8  # System monitoring
cryptography==41.0.7  # Enhanced security
bleach==6.1.0  # HTML sanitization
flask-limiter==3.5.0  # Rate limiting
gunicorn==21.2.0  # Production WSGI server (sync worker for now)
eventlet==0.33.3  # async worker for Flask-SocketIO (recommended)